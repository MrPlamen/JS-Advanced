function calorieObject(arr) {
    let array = [...arr];
    let object = {};
    for (let i = 0; i < array.length; i++) {
        if (i%2 == 0) {
            object[array[i]] = Number(array[i + 1]);
        }
    }
    console.log(object);
